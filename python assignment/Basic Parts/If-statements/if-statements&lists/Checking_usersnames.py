
current_users = ['Justice', 'Love', 'Blessing', 'Oshioke', 'Otaiki', 'Faith', 'pablo']

new_users = ['Otaiki', 'Fave', 'David', 'love', 'Bolaji', 'Pelz', 'Justice', 'PABLO']
current_users1 = current_users[:]
current_users1 = [x.lower() for x in current_users1]
current_users2 = current_users[:]
current_users2 = [x.upper() for x in current_users2]

for new_user in new_users:

	if new_user in current_users : 
		print(f"\nThe name {new_user}, has been used already.")
	elif new_user in current_users1:
		print(f"\nThe name {new_user}, has been used already.")
	elif new_user in current_users2:
		print(f"\nThe name {new_user}, has been used already.")
	else:
		print(f"\nThe name {new_user}, is available.")
	

	








































































	# if new_user in current_users1:
	# 	print(f"The name {new_user}, has been used already.")
	# else:
	# 	print(f"The name {new_user}, is available.")
      
     
	  
	#   if new_user in current_users2:
	#   	print(f"The name {new_user}, has been used already.")
	#   else:
	#   	print(f"The name {new_user}, is available.")

# current_users = ['Justice', 'Love', 'Blessing', 'Oshioke', 'Otaiki', 'Faith', 'pablo']

# new_users = ['Otaiki', 'Fave', 'David', 'love', 'Bolaji', 'Pelz', 'Justice', 'PABLO']


# for new_user in new_users:
# 	if new_user in current_users:
# 		print(f"The name {new_user}, has been used already.")
# 	else:
# 		print(f"The name {new_user}, is available.\n")
# 	current_users1 = current_users[:]
# 	current_users1 = [x.lower() for x in current_users1]
# 	if new_user in current_users1:
# 		print(f"The name {new_user}, has been used already.")
# 	else:
# 		print(f"The name {new_user}, is available.")
      
#       current_users2 = current_users[:]
# 	  current_users2 = [x.upper() for x in current_users2]
# 	  if new_user in current_users2:
# 	  	print(f"The name {new_user}, has been used already.")
# 	  else:
# 	  	print(f"The name {new_user}, is available.")
# current_users = ['Justice', 'Love', 'Blessing', 'Oshioke', 'Otaiki', 'Faith', 'pablo']

# new_users = ['Otaiki', 'Fave', 'David', 'love', 'Bolaji', 'Pelz', 'Justice', 'PABLO']


# for new_user in new_users:
# 	if new_user in current_users:
# 		print(f"The name {new_user}, has been used already.")
# 	else:
# 		print(f"The name {new_user}, is available.\n")
# 	current_users1 = current_users[:]
# 	current_users1 = [x.lower() for x in current_users1]
# 	if new_user in current_users1:
# 		print(f"The name {new_user}, has been used already.")
# 	else:
# 		print(f"The name {new_user}, is available.")
      
#       current_users2 = current_users[:]
# 	  current_users2 = [x.upper() for x in current_users2]
# 	  if new_user in current_users2:
# 	  	print(f"The name {new_user}, has been used already.")
# 	  else:
# 	  	print(f"The name {new_user}, is available.")


















































































































































# for x in current_users1:
# 	print(x)
	    
























		


		# if new_user in current_users1:
		# 	if new_user != x:
		# 		print("this name is available")
		# 	else:
		# 		print("It is available")



































# current_users = ['Justice', 'love', 'Blessing', 'Oshioke', 'Otaiki', 'Faith']

# new_users = ['Otaiki', 'Fave', 'Love' 'David', 'Bolaji', 'Pelz', 'Justice', 'Pablo']

# for name in current_users:
# 		if name in current_users==new_users:
# 				print("this name can't be used")
# 		else:
# 				print("available")










































































































# current_users = ['Justice', 'love', 'Blessing', 'Oshioke', 'Otaiki', 'Faith']

# new_users = ['Otaiki', 'Fave', 'Love' 'David', 'Bolaji', 'Pelz', 'Justice', 'Pablo']




# for new_user in new_users:
# 	if new_user in current_users:
# 		print(f'\nThe name "{new_user}", has already been used')
# 	else:
# 		print(f'\nThe name "{new_user}", is available')
	

# for current_user in current_users:
# 	for new_user in new_users:
# 		if new_user.lower() == current_user:
# 			print(f'the name "{new_user}", has already been used')
# 		else:
# 			print("This name is available")


# for current_user in current_users:
# 	if new_user.upper() == current_user:
# 		print(f'the name "{new_user}", has already been used')
# 	else:
# 		print("This name is available")		

# for current_user in current_users:
# 	if new_user.title() == current_user:
# 		print(f'the name "{new_user}", has already been used')
# 	else:
# 		print("This name is available")



































	# current_user1 = []
# current_user2 = []

# for current_user in current_users:
# 	current_user1.append(current_user.lower())

# for current_user in current_users:
# 	current_user2.append(current_user.upper())

# print(current_user1)
	
